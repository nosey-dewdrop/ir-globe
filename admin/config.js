/* Supabase bağlantısı — Damla doldurur.
   Supabase → Project Settings → API'den al:
   - SUPABASE_URL  = "Project URL"
   - SUPABASE_ANON = "anon public" key
   Bunlar gizli değildir (anon key public'tir); asıl koruma RLS + auth. */
window.SUPABASE_URL = "";
window.SUPABASE_ANON = "";
